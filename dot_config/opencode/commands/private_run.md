---
description: Implement the next pending slice from a tao plan directory
agent: build
---

<!-- tao-managed: run v1 -->

Identify the plan directory from the slash-command arguments or user message. Then run `tao prompt run --plan-dir <plan-directory> --arguments "$ARGUMENTS"` and follow the returned prompt exactly.
