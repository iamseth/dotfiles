---
description: Convert the current planning session into executable serial slices
agent: build
---

<!-- tao-managed: slice v1 -->

Run `tao prompt slice --arguments "$ARGUMENTS"` and follow the returned prompt exactly.
